# Expression

1. `over there` 在那边（同时用手指向那边）

   ```
   Where's the tea?

   It's over there, behind the teapot
   ```

2. `there we are` 好了；就这样；okay

3. `in the evening` 在晚上/在傍晚

4. `here is it` 在这里

5. `here you are` 给你

6. `here they are` 它们在这

7. `run after` 追逐

8. `take picture` 拍照

9. `throw away` 扔掉

10. `put away` 收起

11. `get started` 开始

12. `next door` 隔壁

13. `a minute` 一会儿

14. `to tell you the truth` 说实话

15. `get home` 回家

16. `go out` 外出交际；外出娱乐；与...交往；与...谈恋爱

17. `you two` 你们两个

18. `you all` 你们（三个或三个以上）

19. `writing paper` 信纸

20. `shop assistant` 售货员

21. `take an aspirin` 服用阿司匹林

22. `have a temperature` 发烧

23. `enjoy yourself` 玩得开心

24. `catch the bus` 赶上公交车

25. `the United States of America` 美利坚合众国

26. `country music` 乡村音乐

27. `midnight snack` 夜宵

28. `hundreds of` 数以百计的...

29. `and so on` 等等...

30. `the day before yesterday` 前天

31. `early bird` 早到者

32. `give up` 放弃

33. `once a month` 一个月一次

34. `what's wrong?` 怎么了？

35. `so far` 目前为止；迄今为止

36. `all the time` 始终，一直

37. `never ever` 永远不要

    ```
    Never ever take your eyes off the ball
    ```

38. `some day` 总有一天

39. `turn left` 左转

40. `worth every penny` 物有所值

41. `give him my regards` 代我问候他

42. `you know well` 你认识某人较久

    ```
    A friend is going to visit a girl you know well
    ```

43. `on business` 出差

44. `6 off` 6 折

45. `place order` 下订单

46. `one more time` 再来一次

47. `I have arrived` 我已经到了

48. `punch in` 上班打卡

49. `punch out` 下班打卡

50. `top up` 充值

51. `give a lesson` 授课

52. `pass the exam` 通过考试

53. `zoom in` 放大

54. `zoom out` 缩小

55. `call back` 回电话

56. `stand up` 起立

57. `driving license` 驾照

58. `in face` 事实上

59. `one hour break` 休息一小时

60. `look out` 留神；当心

61. `look for` 寻找

62. `point out` 指出；指明

63. `for a long time` 很长时间以来

    ```
    The English test was easiest we've had for a long time
    ```

64. `cheer up` 加油；振作起来

65. `small change` 零钱

66. `guide dog` 导盲犬

67. `front door` 前门

68. `back door` 后门

69. `just as` 正在...的时候

70. `take the examination` 参加考试

71. `Customer Services` 客服中心

72. `break down` 发生故障

73. `just now` 刚刚

74. `at last` 最后；终于

75. `look up` 查阅

76. `do the shopping` 去购物

77. `look after` 关心；照顾

78. `at least` 至少

79. `more that` 超过

80. `railway station` 火车站

81. `in my opinion` 在我看来

82. `stay up` 熬夜

83. `stay in` 呆在家里

84. `broken in` 强行进入

85. `power off` 关机

86. `get married` 结婚

87. `give up` 放弃

88. `on time` 按时；准时

89. `town hall` 市政厅

90. `most of them` 他们中的大多数

91. `run after` 追逐

92. `this time last year` 去年的这个时候

93. `check in` 办理登记手续

94. `cherry pick` 做出最佳选择

95. `well done` 全熟

96. `afraid of heights` 恐高

    ```
    Are you afraid of heights?
    ```

97. `overlapping sound` 内心独白；内心 os 的全称

98. `as usual` 像往常一样

99. `go away` 离开

100. `but hang on` 但等一下

     ```
     Do you know the thing?

     Yes, but the hang on, let me get the coffee first
     ```

101. `hope so` 但愿如此

102. `lose weight` 体重减轻

103. `I think so` 我想是的

104. `fed up` 感到厌烦

105. `fly in` （乘飞机）降落

106. `she has to travel in her job` 她不得不出差
